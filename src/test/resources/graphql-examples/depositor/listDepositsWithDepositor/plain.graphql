query ListDepositsFromDepositor {
    depositor(id: "user002") {
        deposits(first: 10) {
            edges {
                node {
                    depositId
                    state {
                        label
                        description
                    }
                }
            }
        }
    }
}
