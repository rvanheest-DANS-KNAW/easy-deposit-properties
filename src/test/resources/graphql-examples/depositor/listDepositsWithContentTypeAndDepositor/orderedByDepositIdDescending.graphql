query {
    depositor(id: "user001") {
        deposits(first: 10, contentType: {value: ZIP}, orderBy: {field: DEPOSIT_ID, direction: DESC}) {
            edges {
                node {
                    depositId
                }
            }
        }
    }
}
