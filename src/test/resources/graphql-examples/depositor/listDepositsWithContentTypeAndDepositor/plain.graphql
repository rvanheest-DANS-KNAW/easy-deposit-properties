query {
    depositor(id: "user001") {
        deposits(first: 10, contentType: {value: "application/zip"}) {
            edges {
                node {
                    depositId
                }
            }
        }
    }
}
