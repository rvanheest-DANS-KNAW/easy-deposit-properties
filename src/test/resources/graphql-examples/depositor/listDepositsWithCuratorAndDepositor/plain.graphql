query {
    depositor(id: "user001") {
        deposits(first: 10, curator: {userId: "archie001"}) {
            edges {
                node {
                    depositId
                }
            }
        }
    }
}
