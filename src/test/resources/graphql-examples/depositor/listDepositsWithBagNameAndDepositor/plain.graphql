query {
    depositor(id: "user002") {
        deposits(first: 10, bagName: "bag3") {
            edges {
                node {
                    depositId
                    bagName
                }
            }
        }
    }
}
