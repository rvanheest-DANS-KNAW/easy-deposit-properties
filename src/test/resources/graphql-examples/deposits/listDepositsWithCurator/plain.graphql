query {
    deposits(first: 10, curator: {userId: "archie001", filter: LATEST}) {
        edges {
            node {
                depositId
                curator {
                    userId
                    email
                    timestamp
                }
            }
        }
    }
}
