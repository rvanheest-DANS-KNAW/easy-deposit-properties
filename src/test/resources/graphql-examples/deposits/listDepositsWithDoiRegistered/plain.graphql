query {
    deposits(first: 10, doiRegistered: {value: true, filter: LATEST}) {
        edges {
            node {
                depositId
                doiRegistered
            }
        }
    }
}
