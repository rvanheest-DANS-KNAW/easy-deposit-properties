mutation {
    setDoiAction(input: {
        clientMutationId: "my-mutation-id",
        depositId: "00000000-0000-0000-0000-000000000004",
        value: UPDATE,
#        timestamp uses default
    }) {
        clientMutationId
        doiAction {
            value
            timestamp
        }
    }
}
